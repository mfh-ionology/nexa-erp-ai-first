global 
procedure ISPackageVC_PasteSetupItem(var record ISPackageVc ISPackager)
begin
  record INVc INr;

  INr.Code = ISPackager.SetupItem;
  if (ReadFirstMain(INr,1,true)) then begin
    ISPackager.SetupPrice = INr.UPrice1;
  end;

  return;
end;
