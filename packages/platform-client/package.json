{
  "name": "@nexa/platform-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "ioredis": "^5.4.0",
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "@nexa/config": "workspace:*",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.0",
    "eslint": "^9.39.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
