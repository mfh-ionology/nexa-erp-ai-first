{
  "name": "@nexa/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@nexa/config": "workspace:*",
    "eslint": "^9.39.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
