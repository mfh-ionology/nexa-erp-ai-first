{
  "name": "@nexa/platform-admin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint configured in E0.S4'",
    "test": "echo 'test configured in E0.S4'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@nexa/shared": "workspace:*",
    "@nexa/platform-client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
