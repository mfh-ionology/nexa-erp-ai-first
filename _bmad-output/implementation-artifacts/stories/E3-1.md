# Story E3.1: Event Bus Infrastructure

Status: done

## Story

As a **developer**,
I want a typed, in-process event bus for cross-module communication,
So that modules can publish and subscribe to business events without direct service-to-service coupling.

## Acceptance Criteria

1. GIVEN the EventBus class WHEN I emit a typed event (e.g., `invoice.created`) THEN all registered subscribers for that event are invoked with the correctly typed payload
2. GIVEN the BusinessEvents interface WHEN a developer registers a handler THEN TypeScript enforces the correct payload type for each event name
3. GIVEN an event handler that throws an error WHEN the event is published THEN the error is caught and logged without affecting the emitting module or other subscribers
4. GIVEN the emit() method WHEN called with an event THEN all subscribers execute asynchronously (do not block the emitter)
5. GIVEN a subscriber registration WHEN the same handler is registered twice for the same event THEN it is only invoked once per event emission (deduplication)
6. GIVEN the BusinessEvents interface WHEN new events are added THEN they follow the naming convention `{entity}.{action}` with past tense actions (created, updated, approved, posted)

## Tasks / Subtasks

- [x] **Task 1: Define BusinessEvents interface and event bus types** (AC: #2, #6)
  - [x] 1.1 Create `apps/api/src/core/events/event-bus.types.ts`
  - [x] 1.2 Define `BusinessEvents` interface with all currently-implemented system events migrated from `AppEventMap`:
    - `user.login`: `{ userId: string; loginMethod: string; ipAddress?: string }`
    - `user.mfa.setup`: `{ userId: string }`
    - `user.mfa.enabled`: `{ userId: string }`
    - `user.mfa.reset`: `{ targetUserId: string; resetByUserId: string }`
    - `settings.updated`: `{ key: string; oldValue: string; newValue: string; updatedBy: string }`
  - [x] 1.3 Include access group events (already in use from E2b):
    - `accessGroup.created`: `{ groupId: string; companyId: string; code: string; name: string; createdBy: string }`
    - `accessGroup.updated`: `{ groupId: string; companyId: string; changedBy: string }`
    - `accessGroup.deleted`: `{ groupId: string; companyId: string; deletedBy: string }`
    - `user.accessGroups.assigned`: `{ userId: string; companyId: string; groupIds: string[]; assignedBy: string }`
    - `user.accessGroups.revoked`: `{ userId: string; companyId: string; groupIds: string[]; revokedBy: string }`
    - `company.defaultData.imported`: `{ companyId: string; importedBy: string; version: string }`
  - [x] 1.4 Add placeholder event signatures for future modules as documented in the Event Catalog (all ~93 events from `event-catalog.md` § Event Payload Reference). Use `string` for enum types at this stage per the catalog recommendation (I-08). Group events by module with section comments.
  - [x] 1.5 Follow naming convention: `entity.action` (dot-separated, lowercase, past tense verbs). Three-segment names like `stock.movement.posted` are acceptable for compound entities per Event Catalog naming convention notes.
  - [x] 1.6 Define `EventHandler<K>` type alias: `type EventHandler<K extends keyof BusinessEvents> = (data: BusinessEvents[K]) => void | Promise<void>`
  - [x] 1.7 Export all types and interfaces from the file

- [x] **Task 2: Implement EventBus class** (AC: #1, #3, #4, #5)
  - [x] 2.1 Create `apps/api/src/core/events/event-bus.ts`
  - [x] 2.2 Implement `EventBus` class with:
    - Private `Map<string, Set<Function>>` for handler storage (Set for deduplication — AC #5)
    - Generic `emit<K extends keyof BusinessEvents>(event: K, data: BusinessEvents[K]): void` method
    - Generic `on<K extends keyof BusinessEvents>(event: K, handler: EventHandler<K>): void` method
    - Generic `off<K extends keyof BusinessEvents>(event: K, handler: EventHandler<K>): void` method
    - `removeAllListeners(event?: keyof BusinessEvents): void` method (for test cleanup)
  - [x] 2.3 Implement async handler execution (AC #4):
    - `emit()` must fire handlers asynchronously using `Promise.allSettled()` (or `setImmediate`/`queueMicrotask` per handler)
    - The emitter must NOT await handlers — `emit()` returns `void` immediately
    - Use `queueMicrotask` or `setImmediate` to schedule each handler invocation so the emitting call stack completes first
  - [x] 2.4 Implement error isolation (AC #3):
    - Wrap each handler invocation in a try/catch
    - On handler error, log a structured error using Fastify logger (or `console.error` as fallback) with: event name, handler name/reference, error message, stack trace
    - Failed handlers must NOT affect the emitting module or other subscribers
    - Failed handlers must NOT re-throw errors to the caller
  - [x] 2.5 Implement handler deduplication (AC #5):
    - Use `Set<Function>` per event key to track handlers by reference
    - When `on()` is called, if the handler reference already exists in the Set for that event, skip registration (idempotent `on()`)
    - This prevents the same handler from being invoked multiple times per emission
  - [x] 2.6 Export singleton `eventBus` instance from the module
  - [x] 2.7 Do NOT use Node.js `EventEmitter` internally — implement with plain Maps and Sets for full control over async execution and error handling. The existing `TypedEventEmitter` in `event-emitter.ts` wraps `EventEmitter` which is synchronous — the new EventBus must be async-first.

- [x] **Task 3: Create Fastify plugin for EventBus decorator** (AC: #1)
  - [x] 3.1 Create `apps/api/src/core/events/event-bus.plugin.ts`
  - [x] 3.2 Implement Fastify plugin that registers `eventBus` as a Fastify decorator:
    ```typescript
    import fp from 'fastify-plugin';
    import { eventBus } from './event-bus.js';

    async function eventBusPlugin(fastify) {
      fastify.decorate('eventBus', eventBus);
    }

    export const eventBusPlugin = fp(eventBusPlugin, { name: 'event-bus' });
    ```
  - [x] 3.3 Add TypeScript type augmentation for Fastify:
    ```typescript
    declare module 'fastify' {
      interface FastifyInstance {
        eventBus: EventBus;
      }
    }
    ```
    This makes `request.server.eventBus` type-safe for all modules.
  - [x] 3.4 Register the plugin in `apps/api/src/app.ts` — add `await fastify.register(eventBusPlugin)` BEFORE route registration (after cookie/JWT/company-context, before routes) so all route handlers can access `request.server.eventBus`
  - [x] 3.5 Update the comment block in `app.ts` to reflect the new plugin order

- [x] **Task 4: Migrate existing appEvents usage to EventBus** (AC: #1)
  - [x] 4.1 Update `apps/api/src/core/auth/auth.routes.ts`: replace `import { appEvents } from '../events/event-emitter.js'` with `request.server.eventBus` (accessed via the Fastify decorator). Change all `appEvents.emit(...)` calls to `request.server.eventBus.emit(...)`.
  - [x] 4.2 Update `apps/api/src/modules/system/access-groups.service.ts`: the service receives `eventBus` (or the Fastify instance) as a parameter. Replace `import { appEvents }` and all `appEvents.emit(...)` calls. **Decision:** services that emit events should accept `eventBus: EventBus` as a parameter (dependency injection) so they remain testable without Fastify.
  - [x] 4.3 Update `apps/api/src/modules/system/user-access-groups.service.ts`: same pattern as 4.2 — accept `eventBus: EventBus` parameter, replace `appEvents.emit(...)`.
  - [x] 4.4 Update `apps/api/src/core/rbac/permission-cache-listeners.ts`: replace `import { appEvents }` with the eventBus singleton import. Update `registerPermissionCacheListeners()` to accept `eventBus: EventBus` as parameter (or import the singleton directly). Replace all `appEvents.on(...)` and `appEvents.removeAllListeners(...)` calls.
  - [x] 4.5 Update `apps/api/src/app.ts`: pass `eventBus` (or `fastify.eventBus`) to `registerPermissionCacheListeners(fastify.eventBus)` instead of relying on the module-level singleton.
  - [x] 4.6 **Deprecate** `apps/api/src/core/events/event-emitter.ts`: add a `@deprecated Use EventBus from event-bus.ts instead` JSDoc comment on the `appEvents` export and `TypedEventEmitter` class. Do NOT delete the file yet — E3.2 or a follow-up story will remove it once all usages are confirmed migrated.
  - [x] 4.7 Ensure `AppEventMap` events from `event-emitter.ts` are all present in `BusinessEvents` (verified in Task 1) so the migration is type-safe.

- [x] **Task 5: Update existing tests for EventBus migration** (AC: #1, #3)
  - [x] 5.1 Update `apps/api/src/core/auth/auth.routes.test.ts` and `apps/api/src/core/auth/mfa.routes.test.ts`: mock the EventBus instead of `appEvents`. Since auth routes will now use `request.server.eventBus`, mock the eventBus on the Fastify instance (via `fastify.decorate` or `vi.mock`).
  - [x] 5.2 Update `apps/api/src/core/rbac/permission-cache-listeners.test.ts`: import and use `eventBus` from `event-bus.ts` instead of `appEvents` from `event-emitter.ts`. Update all event emission calls to use the new EventBus.
  - [x] 5.3 Update `apps/api/src/modules/system/access-groups.service.test.ts`: mock `EventBus` instead of `appEvents`. Update mocks to match new service parameter signature (if `eventBus` is now passed as a parameter).
  - [x] 5.4 Update `apps/api/src/modules/system/user-access-groups.service.test.ts`: same pattern as 5.3.
  - [x] 5.5 Verify all migrated tests pass: `pnpm --filter api test`

- [x] **Task 6: Write EventBus unit tests** (AC: #1-#6)
  - [x] 6.1 Create `apps/api/src/core/events/event-bus.test.ts`
  - [x] 6.2 Test: typed emission and subscription — emit a typed event, verify subscriber receives the correctly typed payload (AC #1, #2)
  - [x] 6.3 Test: multiple subscribers — emit an event with 3 registered handlers, verify all 3 are invoked (AC #1)
  - [x] 6.4 Test: error isolation — register a handler that throws, emit the event, verify other handlers still execute and the emitter does not throw (AC #3)
  - [x] 6.5 Test: async error isolation — register a handler that returns a rejected Promise, verify error is caught and logged without affecting other handlers (AC #3)
  - [x] 6.6 Test: async execution — emit an event, verify handlers execute asynchronously (the emit call returns before handlers complete). Use `vi.fn()` to track execution order. (AC #4)
  - [x] 6.7 Test: handler deduplication — register the same handler reference twice for the same event, emit the event, verify the handler is invoked exactly once (AC #5)
  - [x] 6.8 Test: different handler references — register two different handler functions for the same event, verify both are invoked (no false dedup) (AC #5)
  - [x] 6.9 Test: `off()` removes a handler — register, then unregister, emit, verify handler is not invoked
  - [x] 6.10 Test: `removeAllListeners()` clears handlers for a specific event
  - [x] 6.11 Test: `removeAllListeners()` with no argument clears all events
  - [x] 6.12 Test: emitting an event with no subscribers does not throw
  - [x] 6.13 Test: TypeScript type safety — verify that `eventBus.emit('user.login', { wrong: 'payload' })` is a compile-time error (can be a type-level test or documented as a manual verification)

- [x] **Task 7: Write Fastify plugin integration test** (AC: #1)
  - [x] 7.1 Create `apps/api/src/core/events/event-bus.plugin.test.ts`
  - [x] 7.2 Test: after registering the plugin, `fastify.eventBus` is available and is an instance of EventBus
  - [x] 7.3 Test: `request.server.eventBus` is accessible from within a route handler (use Fastify `inject()`)
  - [x] 7.4 Test: events emitted via `fastify.eventBus` are received by subscribers registered on the same EventBus instance

- [x] **Task 8: Verify no regressions and build passes**
  - [x] 8.1 Run full test suite: `pnpm test` (all packages)
  - [x] 8.2 Verify existing E2/E2b tests still pass (auth, MFA, company context, RBAC, user management, access groups, permissions)
  - [x] 8.3 Verify TypeScript compilation: `pnpm --filter api exec tsc --noEmit`
  - [x] 8.4 Verify lint passes: `pnpm lint`
  - [x] 8.5 Verify build passes: `pnpm build`

## Dev Notes

### Critical: Replacing the Placeholder EventEmitter

The existing `apps/api/src/core/events/event-emitter.ts` is a synchronous placeholder (comment on line 98: "Singleton instance — placeholder until E3 event bus"). It wraps Node.js `EventEmitter` which executes handlers synchronously and does not provide error isolation or deduplication.

The new EventBus (this story) replaces it with:
- **Async execution** — handlers are scheduled asynchronously, not blocking the emitter
- **Error isolation** — each handler is wrapped in try/catch, failures are logged but don't propagate
- **Handler deduplication** — Set-based storage prevents duplicate registrations
- **Full BusinessEvents typing** — all ~93 events from the Event Catalog

### Architecture Decision: In-Process for MVP

Per Architecture §4.2: "In-process typed event bus (for MVP monolith)". This is a Node.js-native implementation with no external dependencies (no Redis, no NATS). The future migration path is to replace the in-process EventBus with Redis Streams or NATS — the `BusinessEvents` interface and `EventBus` class API stay the same.

### Async Execution Strategy

The `emit()` method must NOT await handler completion. Handlers should be scheduled via `queueMicrotask()` or `setImmediate()` so the emitting call stack returns immediately. Each handler runs independently and errors are caught per-handler.

```typescript
emit<K extends keyof BusinessEvents>(event: K, data: BusinessEvents[K]): void {
  const handlers = this.handlers.get(event as string);
  if (!handlers || handlers.size === 0) return;
  for (const handler of handlers) {
    queueMicrotask(async () => {
      try {
        await (handler as (data: BusinessEvents[K]) => void | Promise<void>)(data);
      } catch (err) {
        // Structured error logging — handler failure must NOT propagate
        console.error(`[EventBus] Handler error for event "${String(event)}":`, err);
      }
    });
  }
}
```

### Migration Strategy: Gradual, Not Big-Bang

The existing `appEvents` singleton and `event-emitter.ts` are deprecated but NOT deleted in this story. This allows:
- Any code that was merged between E2b reverts and now to still compile
- E3.2 (Audit Trail) can do the final cleanup

Migration pattern for services:
```typescript
// BEFORE (E2b pattern — module-level import)
import { appEvents } from '../../core/events/event-emitter.js';
appEvents.emit('accessGroup.created', { ... });

// AFTER (E3.1 pattern — dependency injection)
export function createAccessGroup(prisma: PrismaClient, eventBus: EventBus, ...): Promise<AccessGroup> {
  // ... business logic ...
  eventBus.emit('accessGroup.created', { ... });
}
```

Migration pattern for route handlers:
```typescript
// BEFORE
import { appEvents } from '../events/event-emitter.js';
appEvents.emit('user.login', { ... });

// AFTER
request.server.eventBus.emit('user.login', { ... });
```

### Handler Deduplication Implementation

Use `Set<Function>` per event key. Since `Set` uses reference equality, two different closures are treated as different handlers even if they have the same body. This is the correct behavior — deduplication prevents the SAME function reference from being registered twice (e.g., in module startup code that runs multiple times).

```typescript
on<K extends keyof BusinessEvents>(event: K, handler: EventHandler<K>): void {
  const key = event as string;
  if (!this.handlers.has(key)) {
    this.handlers.set(key, new Set());
  }
  this.handlers.get(key)!.add(handler); // Set.add is idempotent for same reference
}
```

### Existing Code Patterns to Follow

**Route Plugin Pattern** (from `apps/api/src/core/auth/auth.routes.ts`):
- Import types, use Fastify decorator for event bus access
- Emit events after successful business operations

**Service DI Pattern** (from `apps/api/src/modules/system/access-groups.service.ts`):
- Accept `prisma: PrismaClient` as first argument
- NEW: Accept `eventBus: EventBus` as second argument for services that emit events
- Return typed responses, throw typed errors

**Test Pattern** (from `apps/api/src/core/auth/auth.routes.test.ts`):
```typescript
import { beforeAll, beforeEach, describe, expect, it, vi } from 'vitest';
// Mock EventBus for route tests
// Use vi.hoisted for mocks that need to be at module scope
```

### Protected Files (DO NOT Delete or Overwrite)

- `packages/db/src/client.ts` — PrismaClient singleton
- `packages/db/src/index.ts` — barrel exports
- `apps/api/src/core/rbac/rbac.guard.ts` — existing createRbacGuard
- `apps/api/src/modules/system/index.ts` — ADD to, don't overwrite
- `apps/api/src/core/events/event-emitter.ts` — DEPRECATE, do NOT delete (migration safety)

### Cross-Cutting Patterns (MANDATORY)

- **companyId**: Not applicable — EventBus is infrastructure, not a data entity
- **i18n**: Not applicable — no user-facing strings in event bus
- **Audit**: EventBus is the mechanism that enables audit; the audit service (E3.2) subscribes to it
- **Events**: This IS the event system — self-referential. Every future state change will use this.

### Reference Documents

| Document | Relevant Sections | Key Details |
|----------|------------------|-------------|
| **PRD** | N/A (infrastructure) | Event bus supports FR85 (audit), FR92 (immutable audit) indirectly |
| **Architecture** | §4.2 Event Architecture | In-process typed event bus, BusinessEvents interface, EventBus class with emit/on, event flow example, future migration to Redis Streams/NATS |
| **UX Design Spec** | N/A | No UI in this story |
| **API Contracts** | N/A | Event bus is internal infrastructure, not an API endpoint |
| **Data Models** | N/A | Event bus is in-memory, no database models |
| **Event Catalog** | §Overview, §Event Naming Convention, §Event Payload Reference (full BusinessEvents interface) | entity.action naming, past tense, typed payloads, ~93 event types defined |
| **State Machine** | N/A | Event bus itself is not stateful |
| **Business Rules** | N/A | Event bus is infrastructure |
| **Project Context** | §11 Development Rules | Rule 4: Every state change emits a typed event via event bus; Rule 7: No cross-module direct service calls (use events) |

### Project Structure Notes

New files to create:
```
apps/api/src/core/events/
├── event-bus.types.ts          # NEW: BusinessEvents interface, EventHandler type
├── event-bus.ts                # NEW: EventBus class implementation
├── event-bus.plugin.ts         # NEW: Fastify plugin decorator
├── event-bus.test.ts           # NEW: EventBus unit tests
├── event-bus.plugin.test.ts    # NEW: Fastify plugin integration tests
└── event-emitter.ts            # MODIFY: add @deprecated JSDoc
```

Modified files:
```
apps/api/src/app.ts                                    # MODIFY: register eventBusPlugin, pass eventBus to permission cache listeners
apps/api/src/core/auth/auth.routes.ts                  # MODIFY: use request.server.eventBus instead of appEvents
apps/api/src/core/auth/auth.routes.test.ts             # MODIFY: mock eventBus instead of appEvents
apps/api/src/core/auth/mfa.routes.test.ts              # MODIFY: use eventBus instead of appEvents
apps/api/src/core/rbac/permission-cache-listeners.ts   # MODIFY: accept eventBus parameter
apps/api/src/core/rbac/permission-cache-listeners.test.ts  # MODIFY: use eventBus
apps/api/src/modules/system/access-groups.service.ts   # MODIFY: accept eventBus parameter
apps/api/src/modules/system/access-groups.service.test.ts  # MODIFY: mock eventBus
apps/api/src/modules/system/user-access-groups.service.ts  # MODIFY: accept eventBus parameter
apps/api/src/modules/system/user-access-groups.service.test.ts  # MODIFY: mock eventBus
```

### Source References

- [Source: _bmad-output/implementation-artifacts/epics/epic-E3.md#Story E3.1]
- [Source: _bmad-output/planning-artifacts/event-catalog.md#Event Payload Reference]
- [Source: _bmad-output/planning-artifacts/architecture/core-architectural-decisions.md#§4.2 Event Architecture]
- [Source: _bmad-output/planning-artifacts/project-context.md#§11 Development Rules]
- [Source: apps/api/src/core/events/event-emitter.ts — existing placeholder to replace]

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

### Completion Notes List

- All 8 tasks completed: types definition, EventBus class, Fastify plugin, migration from appEvents, test updates, unit tests, plugin integration tests, regression verification
- All task checkboxes marked complete
- Code review completed (2026-02-21) — 3 HIGH, 5 MEDIUM, 3 LOW issues identified for future follow-up (see Code Review Notes section below)
- BusinessEvents interface covers ~87 event types from Event Catalog
- Existing appEvents/TypedEventEmitter deprecated (not deleted) for migration safety
- EventBus registered as Fastify decorator, accessible via `request.server.eventBus`
- Services migrated to dependency injection pattern (accept `eventBus: EventBus` parameter)

### File List

**New files created:**
- `apps/api/src/core/events/event-bus.types.ts` — BusinessEvents interface, EventHandler type
- `apps/api/src/core/events/event-bus.ts` — EventBus class implementation
- `apps/api/src/core/events/event-bus.plugin.ts` — Fastify plugin decorator
- `apps/api/src/core/events/event-bus.test.ts` — EventBus unit tests
- `apps/api/src/core/events/event-bus.plugin.test.ts` — Fastify plugin integration tests

**Modified files:**
- `apps/api/src/app.ts` — registered eventBusPlugin, pass eventBus to permission cache listeners
- `apps/api/src/core/auth/auth.routes.ts` — migrated to request.server.eventBus
- `apps/api/src/core/auth/auth.routes.test.ts` — mock eventBus instead of appEvents
- `apps/api/src/core/auth/mfa.routes.test.ts` — mock eventBus instead of appEvents
- `apps/api/src/core/events/event-emitter.ts` — added @deprecated JSDoc
- `apps/api/src/core/rbac/permission-cache-listeners.ts` — accept eventBus parameter
- `apps/api/src/core/rbac/permission-cache-listeners.test.ts` — use eventBus
- `apps/api/src/modules/system/access-groups.service.ts` — accept eventBus parameter
- `apps/api/src/modules/system/access-groups.service.test.ts` — mock eventBus
- `apps/api/src/modules/system/user-access-groups.service.ts` — accept eventBus parameter
- `apps/api/src/modules/system/user-access-groups.service.test.ts` — mock eventBus


## Code Review Notes (Auto-Generated)

**Status:** Completed with remaining issues after 3 CR iterations

**Date:** 2026-02-21 09:09

### Remaining Issues for Human Review:

- ISSUE #1: [HIGH] `drain()` infinite loop on cascading event cycles. In `event-bus.ts:123-127`, if handler A emits event B and handler B emits event A, new promises are continuously added to `this.pending`, creating an infinite loop that hangs graceful shutdown. No cycle detection, max iteration count, or timeout guard.
- ISSUE #2: [HIGH] `permission-cache-listeners.ts` fire-and-forgets async work — invisible to `drain()`. In lines 33-37, handlers call `permissionService.invalidateGroup(...).catch(...)` but do NOT return the promise. The handler returns `void`, so the EventBus sees no async work to track. During shutdown, `drain()` completes immediately while `invalidateGroup()` DB calls are still in-flight. Cache invalidation silently lost.
- ISSUE #3: [HIGH] Event payloads passed by reference — mutation risk across concurrent handlers. In `event-bus.ts:93`, the same `data` object reference is passed to all handlers via `queueMicrotask`. If handler A mutates a field (e.g., `data.userId = 'x'`), handler B receives the mutated payload. No `Object.freeze()` or shallow copy. For financial events carrying amounts, this is a data integrity risk.
- ISSUE #4: [MEDIUM] `permission-cache-listeners.ts` missing `user.accessGroups.revoked` handler. BusinessEvents defines `user.accessGroups.revoked` (event-bus.types.ts:71-76), and the cache listener handles `.assigned` but NOT `.revoked`. When revoke functionality is implemented, stale permissions will persist in cache until natural expiry.
- ISSUE #5: [MEDIUM] `once()` handlers cannot be cancelled via `off()`. In `event-bus.ts:43-52`, `once()` creates an internal wrapper. The caller has no reference to it, so `bus.off(event, originalHandler)` does nothing. The only escape is `removeAllListeners(event)` which nukes all handlers for that event.
- ISSUE #6: [MEDIUM] No handler execution timeout. A handler awaiting a never-resolving promise blocks `drain()` indefinitely. No timeout or watchdog. In production, a single hung handler during `onClose` prevents Fastify shutdown, leading to process kill and potential data loss.
- ISSUE #7: [MEDIUM] `once()` is out of scope — not in acceptance criteria or task list. Story AC defines `on()`, `off()`, `emit()`, `removeAllListeners()`. Task 2.2 does not mention `once()`. Added without story authorization, increases API surface, and introduces the cancellation bug in Issue #5.
- ISSUE #8: [MEDIUM] `approval.autoEscalated` naming mismatch with Event Catalog. The Event Catalog defines `approval.auto_escalated` (underscore). The types file (event-bus.types.ts:558) uses `approval.autoEscalated` (camelCase). Inconsistency between canonical spec and implementation violates Document Synchronisation Rule.
- ISSUE #9: [LOW] Story claims "~93 events" but actual count is ~87. Task 1.4 states "all ~93 events from event-catalog.md" but the implemented BusinessEvents interface contains approximately 87 event types. The task description is misleading.
- ISSUE #10: [LOW] `once()` not tested for error isolation. No test verifies that a `once()` handler that throws is properly caught, logged, and isolated. Existing error tests (6.4, 6.5) only cover `on()` handlers. Different code path deserves independent verification.
- ISSUE #11: [LOW] No runtime payload validation. The EventBus relies entirely on compile-time TypeScript checks. At runtime, `emit('user.login', { wrong: 'payload' } as any)` succeeds silently. Becomes a problem when migrating to Redis Streams/NATS where payloads are serialized.
- Summary: 3 HIGH, 5 MEDIUM, 3 LOW issues found

---

