# Nexa AI Engine — Contract (Pre‑Migration, Read‑Only)

Scope (this phase)
- Assist + query only. No writes or side‑effects.
- Answers are strictly data‑backed via Nexa’s own APIs/DB/file stores.
- If the system cannot answer truthfully, it returns HTTP 501 with:
  - `{ ok: false, reason: "unsupported_or_insufficient_data", message: "…" }`
  - No “best guesses”, no fabrication.

Security & Access
- Tenant context from session is required.
- RBAC and module toggles enforced per intent:
  - Finance: `ui:finance_reports:view`
  - CRM: `crm:manage`
  - Planning: `planning:manage`
  - Workflow: `workflow:manage`
  - Custom fields: `custom:manage`
  - Healthcare: `healthcare:manage`
  - Inventory/Supply/Manufacturing/Projects/POS/HR: MANAGER+ (read‑only) with module toggle enabled.
- Dimension scoping applied to inventory/WMS intents (`applyInventoryScope`).
- Rate limited via `rateLimitTenant("ai-query", tenantId, userId)`.
- Audit logs are not generated by AI intents in this read‑only phase.
- No secrets or prompts are logged.

Request/Response
- Endpoint: `POST /api/ai/query`
- Request body: `{ prompt: string, pathname?: string }` (Shell sends `prompt`).
- Success (200): `{ ok: true, intent: string, source: string, data: object }`
- Refusal (501): `{ ok: false, reason: "unsupported_or_insufficient_data", message: string }`
- Unauthorized (401/403): `{ ok: false, reason: "unauthorized", message: string }`

Intents (read‑only)
- Finance
  - `finance.journals.summary` → source: `finance.journals`
    - Data: `{ countLast30Days: number, recent: Array<{ action, at, target }> }`
- Inventory/WMS
  - `inventory.on_hand_by_item` → source: `inventory.items` (dimension‑scoped)
    - Data: `{ rows: Array<{ sku, totalOnHand }> }`
- Supply Chain
  - `supply.stockout_and_suggestions` → source: `supply.replenishment`
    - Data: `{ risks: [...], suggestions: [...] }`
- Manufacturing
  - `manufacturing.wip_overview` → source: `manufacturing.wip`
    - Data: `{ counts: Record<status, number>, total: number }`
- Sales/CRM
  - `crm.pipeline_summary` → source: `crm.opportunities_quotes`
    - Data: `{ stages: Record<stage, { count, amountMinor }>, openQuotes: number }`
- POS
  - `pos.sales_today_total` → source: `pos.sales`
    - Data: `{ currency: "GBP", totalMinor: number, count: number }`
- Projects
  - `projects.timesheets.summary` → source: `projects.audit`
    - Data: `{ created: number, approved: number }`
- HR/Payroll
  - `hr.snapshot` → source: `hr.audit+users`
    - Data: `{ headcount: number, payrollRuns: number }`
- Healthcare
  - `healthcare.rota_coverage` → source: `healthcare.rota`
    - Data: `{ total: number, upcoming: number }`
- Planning
  - `planning.budgets.summary` → source: `planning.budgets`
    - Data: `{ year: string, count: number, totalMinor: number }`
- Workflow
  - `workflow.pending_approvals` → source: `workflow.instances`
    - Data: `{ total: number, byDefinition: Record<definitionId, number> }`
- Admin/Integrations
  - `admin.live_checks` → source: `diag.env` (non‑secret)
    - Data: `{ openai: "configured"|"not_configured", stripe: "configured"|"not_configured" }`

UX
- AI bar appears on every authenticated (app) page via the shared Shell.
- Placeholder is context‑aware (module‑specific example prompts).
- 429 → “rate‑limited” message.
- 501 → “cannot answer” message; no free‑form guesses.

E2E Coverage (added in this pass)
- Presence: `/finance`, `/inventory`, `/manufacturing`, `/sales`, `/projects`, `/hr`, `/pos`, `/ai`, `/supply/dashboard`, `/crm/leads`, `/planning/budgets`, `/workflow/definitions`, `/healthcare/rota`, `/admin/users`.
- Truthfulness (read‑only):
  - Finance journals summary
  - Supply stock‑out risks
  - CRM pipeline summary
  - POS today sales
  - Planning budgets summary
  - Negative/unsupported prompt → 501


